# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'BinaryProtocol' do
 
  # Pods for BinaryProtocol

  target 'BinaryProtocolTests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'BinaryProtocolPackageDescription' do

end
